<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/HugContent.SmallInnerBarrier"
    android:layout_width="match_parent">

    <ImageView
        android:id="@+id/image_dropdown_icon"
        style="@style/HugContent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_and" />

    <TextView
        android:id="@+id/tv_gate_name"
        style="@style/ConstraintContainer.OuterBarrier.ONLY_START"
        android:layout_height="wrap_content"
        android:theme="@style/TextPrimary.Dark.Heading4"
        app:layout_constraintBottom_toBottomOf="@id/image_dropdown_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/image_dropdown_icon"
        app:layout_constraintTop_toTopOf="@id/image_dropdown_icon"
        tools:text="@string/hint_gate_list" />

</androidx.constraintlayout.widget.ConstraintLayout>
